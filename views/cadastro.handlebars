<style>
    body { 
        background-image: url('/assets/img/the_netflix_login_background__canada__2024___by_logofeveryt_dh0w8qv-pre.jpg');
        background-size: cover; 
        background-color: #000; color: #fff;
    }
    body::before {
        content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0;
        background: rgba(0, 0, 0, 0.5); z-index: -1;
    }
    .login-container { display: flex; justify-content: center; align-items: center; height: 100vh; }
    .login-box { background-color: rgba(0, 0, 0, 0.75); padding: 60px; border-radius: 8px; max-width: 450px; width: 100%; }
    .form-control { background-color: #333; border: none; color: #fff; height: 50px; margin-bottom: 15px;}
    .btn-login { background-color: #e50914; border: none; font-weight: bold; width: 100%; padding: 10px; margin-top: 10px;}
</style>

<div class="login-container">
    <div class="login-box">
        <h1>Criar Conta</h1>
        <form action="/create_user" method="POST">
            <input type="text" name="username" class="form-control" placeholder="Nome de Usuário" required>
            <input type="email" name="email" class="form-control" placeholder="Email" required>
            <input type="password" name="password" class="form-control" placeholder="Senha" required>
            <button type="submit" class="btn btn-login">Cadastrar</button>
        </form>
        <div class="mt-3 text-center">
            <a href="/" style="color: #ccc; text-decoration: none;">Voltar para Login</a>
        </div>
    </div>
</div>